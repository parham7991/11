# آپدیت سوم اسمبل آنلاین هوشمند

## تغییرات مهم

### تحلیل هوش مصنوعی دقیق‌تر شد
- پیام بدِ قبلی که می‌گفت `AI_CHAT_PROVIDER` و `AI_CHAT_API_KEY` را تنظیم کنید حذف شد.
- اگر AI آنلاین به هر دلیلی جواب ندهد، به‌جای پیام خام تنظیمات، یک تحلیل تخصصی داخلی و قابل‌استفاده نمایش داده می‌شود.
- اگر پروکسی فعال باشد ولی وصل نشود، سیستم یک بار مستقیم هم AI را تست می‌کند تا بی‌دلیل fallback نشود.
- اگر کاربر اشتباهاً کلید را با `NEXT_PUBLIC_AI_CHAT_API_KEY` گذاشته باشد هم خوانده می‌شود، ولی پیشنهاد اصلی همچنان `AI_CHAT_API_KEY` است.

### چند RAM و چند SSD قوی‌تر شد
- اگر مادربرد اسلات آزاد داشته باشد، اسمبلر می‌تواند چند SSD اضافه کند.
- اگر مدل SSD مناسب دیگری پیدا نشود، همان SSD منتخب با `quantity` بیشتر استفاده می‌شود تا اسلات‌های آزاد و بودجه بهتر مصرف شوند.
- هدف حافظه برای بودجه‌های بالا افزایش پیدا کرد؛ برای گیمینگ/ادیت/استریم سیستم‌های خفن‌تر حافظه بیشتری می‌گیرند.
- RAM حالا فقط دوبرابر نمی‌شود؛ بر اساس هدف 32/64/128GB و بودجه، quantity رم تا چند کیت افزایش پیدا می‌کند.

### تحلیل AI حالا quantity را هم می‌فهمد
- وقتی RAM یا SSD چندتایی است، در پرامپت تحلیل AI تعداد و توضیح آن ارسال می‌شود.
- تحلیل نهایی دقیق‌تر درباره مجموع RAM و مجموع حافظه نظر می‌دهد.

## تست

```bash
npx tsc --noEmit
```

نتیجه: بدون خطا.
