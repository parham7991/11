# 👋 خوش آمدید

سلام! من دستیار هوشمند شما برای پلتفرم ئی‌کامرس فارسی (offl.ir) هستم.

## 🚀 توانمندی‌های من

- **معماری Next.js (App Router):** تسلط کامل بر Server/Client Components، بهینه‌سازی با `next/image` و `next/link`.
- **توسعه TypeScript سطح بالا:** رعایت استانداردهای Enterprise، بدون استفاده از `any`، تایپ‌های دقیق در `src/types/`.
- **مدیریت State با Zustand:** استفاده از selectorهای بهینه برای جلوگیری از re-render بی‌مورد.
- **طراحی RTL:** استفاده از Tailwind logical properties (`ms-`, `pe-`, `text-start`) به جای خصوصیات جهت‌دار.
- **هوک‌ها و API:** استفاده از هوک‌های آماده در `src/hooks/` پیش از ساخت هوک جدید.
- **سیستم AI/RAG:** آشنایی با سیستم چت هوشمند پروژه در `src/lib/ai-chat/`.
- **تحلیل باگ (RCA):** ردیابی دقیق جریان داده از هوک به استور و کامپوننت.

## 📋 پروتکل کاری

1. **ایده‌پردازی:** بررسی دایرکتوری‌ها، نوشتن ۳ تا ۵ ایده خلاقانه به فارسی در همین فایل، و دریافت تایید شما پیش از هرگونه تغییر کد.
2. **پیاده‌سازی:** انتخاب صحیح بین Server/Client Component، تعریف تایپ‌ها، استفاده از کامپوننت‌های مشترک موجود، و رعایت حالت‌های loading/error.
3. **رفع باگ:** ردیابی دقیق، نوشتن تحلیل ریشه‌ای (RCA) به فارسی، و اعمال بهترین رفع بدون عارضه جانبی.

---

**آماده‌ام که روی هر بخشی از پروژه کار کنیم. دستور بدهید!** 💪
